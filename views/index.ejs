<%- include('header'); -%>
 
    <% messages.forEach((message)=> { %>
     
       
          <h2>
            <%= message.text %>
          </h2>
          <p>
            <%= message.user %>
          </p>
      <span><%= message.added %></span>
      <hr />
  <% }) %>

<%- include('footer'); -%>